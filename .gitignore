# Secrets — never commit real env files
.env
.env.local
.env.*.local
backend/.env
frontend/.env.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
*.egg-info/

# Node / Next.js
node_modules/
frontend/node_modules/
.next/
out/
*.log
.next/
.vercel

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Impeccable local overrides (keep shared skill, ignore machine-local config)
.impeccable/config.local.json
# Impeccable engine binaries are machine-specific — reinstall with:
#   npx impeccable install --providers=opencode --scope=project
.opencode/skills/*/scripts/bin/

# Test / private data — resumes must NEVER be committed
*.pdf
*.docx
!docs/**/*.pdf
private-data/
